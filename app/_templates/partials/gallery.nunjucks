<!-- gallery.nunjucks -->  
  <div class="gallery">
    <!-- Loops through "images" array -->
    {% for image in homepage.gallery %}
    <div class="gallery__item">
      <img src="{{image.src}}" alt="{{image.alt}}">
    </div>
    {% endfor %}
  </div>