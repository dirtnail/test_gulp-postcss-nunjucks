{# gallery.nunjucks #}
  <div class="gallery">
  {# Loops through "images" array #}
    {% for image in content.home.gallery %}
    <div class="gallery__item">
      <img src="{{image.src}}" alt="{{image.alt}}">
    </div>
    {% endfor %}
  </div>